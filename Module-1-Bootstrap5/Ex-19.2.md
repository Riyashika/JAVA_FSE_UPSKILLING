Bootstrap Customization using Sass

1. Install Bootstrap and Sass.

npm install bootstrap
npm install sass

2. Create a custom.scss file.

3. Override Bootstrap variables.

Example:

$primary: green;

4. Import Bootstrap.

@import "bootstrap/scss/bootstrap";

5. Compile Sass file to CSS.

sass custom.scss custom.css

The generated CSS file will contain customized Bootstrap styles.